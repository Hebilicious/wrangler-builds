# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).

Useful commands:

- `moon run workspace:changeset` to create a new changeset
- `moon run workspace:changeset-status` to inspect the pending release plan
- `moon run workspace:version` to apply pending version bumps locally
